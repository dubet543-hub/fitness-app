# Admin web (Vite) environment — copy to .env and adjust as needed.
# Same-origin default (backend serves the admin build):
VITE_API_BASE_URL=/api
# Separate host example:
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
